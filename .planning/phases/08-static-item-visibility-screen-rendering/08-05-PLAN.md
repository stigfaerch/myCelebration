---
phase: 8
plan: 08-05
wave: 2
depends_on: [08-02]
agent: engineering-senior-developer
files_modified:
  - src/components/screen/ScreenPageCycle.tsx
  - src/components/screen/ScreenDeltagere.tsx
  - src/components/screen/ScreenHvor.tsx
  - src/components/screen/ScreenOpgaver.tsx
  - src/components/screen/ScreenProgram.tsx
  - src/app/[uuid]/page.tsx
must_haves:
  - ScreenPageCycle becomes polymorphic — cycles through MixedAssignment items (page OR static)
  - New screen renderers exist for Deltagere, Hvor, Opgaver, Program (Galleri reuses existing ScreenDefault)
  - Strict-blank invariant preserved (assignments exist but zero visible → black screen, NOT gallery default)
  - Realtime subscription continues working for both add AND remove events (DELETE coverage from migration 009)
  - Existing dynamic-page cycling behavior unchanged when only page-kind assignments are present
---

<objective>
Make the screen render polymorphic — given a screen with a mixed list of dynamic-page and static-item assignments, cycle through them in a single visual rotation with the same fade/slide/none transitions. Build the four missing screen renderers (Deltagere, Hvor, Opgaver, Program) that the cycler invokes for static slots.
</objective>

<context>
- Read 08-CONTEXT.md and 08-02-SUMMARY.md to know which polymorphic schema was chosen
- Existing `ScreenPageCycle` accepts `initialPages: { id, title, content }[]`. This plan generalises to `initialItems: MixedScreenItem[]` where each item is either `{ kind: 'page', page: {...} }` or `{ kind: 'static', staticKey, data: {...} }`. The cycle interval, transition logic, double-render layered transition mechanics — all reused as-is. Only the per-item view component differs.
- Existing screen renderers (relevant): `ScreenDefault.tsx` (gallery, full feature), `ScreenPage.tsx` (TipTap page), `ScreenPhoto.tsx`, `ScreenMemory.tsx`. The four new ones (`ScreenDeltagere`, `ScreenHvor`, `ScreenOpgaver`, `ScreenProgram`) follow the same fullscreen-on-a-TV visual pattern: dark background (`bg-slate-950`), large readable typography, no chrome
- Each static screen renderer needs server-side data:
  - **ScreenDeltagere**: list of non-screen guests (`getGuests` filtered) — show name + type label, possibly avatar/initial — laid out for fullscreen readability
  - **ScreenHvor**: events list (same data as `/hvor` page) — show name, time, address; for fullscreen, embed Google Maps iframe at large scale (drop the bottom-sheet wrapper used in mobile)
  - **ScreenOpgaver**: tasks list — show titles, assigned guests, time. Read-only, no actions.
  - **ScreenProgram**: program timeline — same data as `/program` page, but typography scaled for screen viewing
- Server vs client: the cycler (ScreenPageCycle) is a client component because it manages timers and realtime. Per-cycle item rendering can be either:
  - Pre-fetched on the server when assembling `initialItems` (simpler, but means the client component receives all the data up-front)
  - Lazy-fetched per item via `useSWR` or similar (smaller initial payload, but adds complexity and deps the project doesn't have)
  Recommended: pre-fetch on the server, mirror the existing pattern for `ScreenPageForCycle` (which already includes `content`)
- Realtime: existing subscription on `screen_page_assignments` (post-migration 011 with REPLICA IDENTITY FULL) catches both kinds because they live on the same table (assuming Decision B1). On any change, the cycler refetches both pages AND static items via `getVisibleScreenAssignmentsMixed`. Decision B2 would require subscribing to TWO topics — handle accordingly per the schema chosen
- The screen branch in `src/app/[uuid]/page.tsx` (line ~38) currently calls `hasAnyScreenAssignments` then assembles a `ScreenPageCycle`. Update to use the mixed-assignment exports from 08-02
</context>

<tasks>

<task id="1" title="Create the four new screen renderers">
<action>
1. Create `src/components/screen/ScreenDeltagere.tsx`. Receives `guests: { id, name, type }[]` as a prop. Render fullscreen layout: dark bg, large heading "Deltagere", grid/list of names with their type label as small caption. Filter out screens (already done at fetch level). 5xl+ typography. Use the existing CyclePageView container styling pattern from `ScreenPageCycle` (`absolute inset-0 overflow-auto bg-slate-950 text-white` etc.).

2. Create `src/components/screen/ScreenHvor.tsx`. Receives `events: { id, name, address, start_time, ... }[]`. Render fullscreen: each event card shows name, time, address, and the Google Maps iframe embedded at fullscreen scale. If multiple events, scroll vertically OR show one event per cycle step (simpler — but means the cycler treats `hvor` as one item, so multiple events all render in one slide). Pick the simpler approach: one composite slide showing all events at once, scrollable.

3. Create `src/components/screen/ScreenOpgaver.tsx`. Receives `tasks: { id, title, due_time, location, max_persons, assignments: {...}[] }[]`. Read-only fullscreen list. Show task title prominently, assigned guest names below.

4. Create `src/components/screen/ScreenProgram.tsx`. Receives `items: ProgramRow[]` (same shape as existing `/{uuid}/program` page uses). Render the timeline at fullscreen typography, parent-child nesting preserved.

5. All four follow the same visual conventions: dark background, white text, large typography (5xl headings, lg/xl body), centered or full-width layout depending on content density.

6. NONE of these access realtime, server actions internally, or use 'use client' — they're pure presentational components receiving data as props.
</action>
<verify>npx tsc --noEmit 2>&1 | grep -v "^$" | head -20 || echo "tsc clean"</verify>
<done>Four new screen renderers exist as pure presentational components.</done>
</task>

<task id="2" title="Refactor ScreenPageCycle to be polymorphic">
<action>
1. Read `src/components/screen/ScreenPageCycle.tsx`.

2. Define a new discriminated-union item shape:
```ts
export type MixedScreenItem =
  | { kind: 'page'; id: string; title: string; content: Record<string, unknown> | null }
  | { kind: 'static'; id: string; staticKey: string; data: unknown }  // narrow `data` per staticKey via runtime check
```

3. Rename prop `initialPages` → `initialItems`. The cycle interval / transition / double-render mechanics stay identical — they only operate on indices into the `items` array.

4. Update the inner `CyclePageView` component (currently renders `ScreenPage` content): make it dispatch by `item.kind`:
   - `'page'` → existing TipTap title + RichTextDisplay content
   - `'static'` → switch on `item.staticKey`:
     - `'galleri'` → `<ScreenDefault config={...} items={...} />` (reuse existing component; `data` carries config + items)
     - `'deltagere'` → `<ScreenDeltagere guests={data.guests} />`
     - `'hvor'` → `<ScreenHvor events={data.events} />`
     - `'tasks'` → `<ScreenOpgaver tasks={data.tasks} />`
     - `'program'` → `<ScreenProgram items={data.items} />`
     - any other static_key → render an empty fullscreen-black div (defensive)

5. Update the realtime refetch handler to call `getVisibleScreenAssignmentsMixed` (from 08-02) AND a new helper `getMixedScreenItemPayload(screenGuestId)` that hydrates the `data` field for each static item by fetching its data from the appropriate source (guests, events, tasks, program_items). Either inline the hydration in the existing `screenAssignments.ts` file or split into a new module — your call.

6. Strict-blank preserved: when `items.length === 0` after refetch, render `<div className="h-screen w-screen bg-black" />` exactly as today.

7. The `animateIn` initial-true fix from the recent ScreenPageCycle bug fix MUST be preserved. Don't regress it.
</action>
<verify>npx tsc --noEmit 2>&1 | grep -v "^$" | head -20 || echo "tsc clean"</verify>
<done>ScreenPageCycle accepts mixed items and renders the right component per kind. Existing fade/slide/none transitions still work. Strict-blank still works.</done>
</task>

<task id="3" title="Update the screen branch in src/app/[uuid]/page.tsx">
<action>
1. Read the screen branch (~lines 38–131 of `src/app/[uuid]/page.tsx`).

2. Replace the existing `hasAnyScreenAssignments` + `getVisibleScreenAssignments` flow with the mixed-assignment equivalents:
   - `hasAnyScreenAssignments` (already counts both kinds per 08-02 update) — no change
   - `getVisibleScreenAssignments` → `getVisibleScreenAssignmentsMixed`
   - Hydrate the static items' `data` fields server-side: for each item with `kind: 'static'`, fetch the appropriate source (galleri config + items, guests list, events list, tasks list, program items)

3. Pass the resulting `MixedScreenItem[]` to `<ScreenPageCycle initialItems={items} ... />`.

4. The non-pages-mode branches (single-override `screen_state.current_override`, gallery default fallback) are untouched.

5. Server-component fetch pattern: parallelise fetches with `Promise.all` to keep render time bounded.
</action>
<verify>npx tsc --noEmit 2>&1 | grep -v "^$" | head -20 || echo "tsc clean"</verify>
<done>Screen branch produces mixed items, including hydrated static-item data, fed into the polymorphic cycler.</done>
</task>

<task id="4" title="Verify regressions and document realtime behavior">
<action>
1. `npx tsc --noEmit` clean.
2. Mental walk-through: a screen with only page-kind assignments cycles dynamic pages exactly as before (verify no behavior shift in the fade/slide/none transitions, no shift in cycle interval semantics).
3. A screen with only static-kind assignments (e.g., admin assigned only Galleri) renders that static view continuously (no cycling) since `pages.length === 1`.
4. A screen with mixed (e.g., Galleri + Program + a dynamic page) cycles all three.
5. Realtime: removing a static assignment via admin triggers the existing channel subscription (Decision B1 — same table). Confirm by tracing the channel filter.
</action>
<verify>npx tsc --noEmit 2>&1 | tail -3</verify>
<done>Type-check clean; no regression in dynamic-page-only path; mixed and static-only paths render correctly.</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` exits 0
- Four new screen renderers exist and compile
- `ScreenPageCycle` accepts mixed items
- `[uuid]/page.tsx` screen branch fetches mixed assignments and hydrates static-item data server-side
- Strict-blank invariant preserved (zero visible items → black)
- `animateIn` initial-true fix preserved
- Realtime channel still catches DELETE (REPLICA IDENTITY FULL from migration 009 covers this for the polymorphic table)
</verification>

<success_criteria>
- Admin assigning Galleri to primary screen via 08-03 admin UI: screen renders the gallery using existing `ScreenDefault`
- Admin assigning Program: screen renders timeline at fullscreen typography
- Admin assigning Deltagere: screen renders guest list at fullscreen typography
- Admin assigning Hvor: screen renders events with embedded maps
- Admin assigning Opgaver: screen renders task list
- Mixed cycle (e.g., Galleri + a dynamic Welcome page) cycles between them every cycle_seconds with fade
</success_criteria>

<output>
Write `.planning/phases/08-static-item-visibility-screen-rendering/08-05-SUMMARY.md` with:
- **Status**: Complete | Failed
- **Files modified/created**: absolute paths
- **Polymorphic prop shape**: the final `MixedScreenItem` discriminated union, posted verbatim
- **Hydration approach**: where the static-item `data` is fetched (in `[uuid]/page.tsx`? in a new helper in `screenAssignments.ts`?). Note any per-render cost considerations.
- **Realtime notes**: confirmation that the existing channel covers both kinds (Decision B1) OR description of any new subscription added (Decision B2)
- **Verification**: tsc result, mental-walkthrough trace for the 5 success-criteria scenarios
</output>
