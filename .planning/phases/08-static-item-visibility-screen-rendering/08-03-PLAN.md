---
phase: 8
plan: 08-03
wave: 2
depends_on: [08-01, 08-02]
agent: engineering-frontend-developer
files_modified:
  - src/components/admin/MenuManager.tsx
  - src/components/admin/ScreenAssignmentToggle.tsx
  - src/components/admin/StaticItemVisibilityControls.tsx
  - src/app/(admin)/admin/sider/page.tsx
must_haves:
  - Static rows in MenuManager get visibility controls (is_active toggle, visible_from/visible_until inputs) with same EyeOff/Clock indicators as dynamic pages
  - Static rows in MenuManager (subset: Galleri, Deltagere, Hvor, Opgaver, Program) get the screen-toggle Monitor button — same UX as dynamic-page toggle
  - Kamera and Billeder rows: visibility controls YES, screen-toggle NO (visually hidden / disabled)
  - Auto-save on visibility change (debounced like ScreenCycleSettingsAutoSave pattern)
  - dnd-kit drag still works on rows after the new controls are added (don't break the drag handle)
  - All Danish text consistent with existing admin labels
---

<objective>
Surface visibility controls and screen-activation on static rows of `/admin/sider`, bringing them to UX parity with dynamic page rows. Keep Kamera and Billeder out of screen-toggle scope.
</objective>

<context>
- Read 08-CONTEXT.md and the SUMMARY.md files from 08-01 and 08-02 to know which storage layouts were chosen
- Plan 08-01 added `staticItemVisibility.ts` with `getStaticItemVisibilityMap`, `updateStaticItemVisibility`, types
- Plan 08-02 extended `screenAssignments.ts` with `addStaticItemToScreen`, `removeStaticItemFromScreen`, `getStaticAssignmentsMapByKey`
- Existing `MenuManager.tsx` renders rows differently for `kind: 'static'` vs `kind: 'page'`. Static rows currently show: drag handle + icon (from `ICON_BY_KEY`) + label (translated via `useTranslations('guest.nav')`) + "Indbygget" badge. No actions. — This plan adds actions.
- Existing `ScreenAssignmentToggle.tsx` accepts `pageId: string` prop. This plan extends it to a discriminated union: either `{ pageId: string }` or `{ staticKey: string }`. Internal click/long-press logic stays the same — only the action call changes (`addPageToScreen` vs `addStaticItemToScreen`). Suggest renaming the prop interface but keeping the export name stable to avoid churn at call sites that already exist for pages.
- Existing visibility indicators on dynamic pages live inline in MenuManager — EyeOff icon when `!is_active`, Clock when outside visibility window. This plan extracts/reuses that visual treatment for static rows. Consider extracting a tiny `<VisibilityBadges {...visibility} />` component if it improves clarity.
- The actual editable controls (is_active checkbox + datetime-local inputs for visible_from/visible_until) need a new component because dynamic pages get edited in `PageForm.tsx` via a separate flow. For static rows we want INLINE editing (no modal, no separate page) — debounced auto-save on change. Pattern reference: `ScreenCycleSettingsAutoSave` in `ScreenCycleSettings.tsx`.
- The sider admin page (`src/app/(admin)/admin/sider/page.tsx`) needs to also fetch the visibility map and pass it down so MenuManager can render current state.
- Static keys NOT eligible for screen-toggle: `'camera'`, `'photos'` (reasoning: camera needs camera access, photos is per-guest content). Disable/hide the Monitor button for these two keys. The other 5 (`tasks`, `program`, `hvor`, `deltagere`, `minder`, `galleri`) — wait: actually `tasks` and `minder` weren't in the original Phase 8 list. Re-check ROADMAP/PROJECT: scope is **Galleri, Deltagere, Hvor, Opgaver, Program**. Five keys. So eligible static keys for screen-toggle: `galleri`, `deltagere`, `hvor`, `tasks`, `program`. Ineligible: `camera`, `photos`, `minder`. Confirm this matches PROJECT.md R25 wording before implementation.
</context>

<tasks>

<task id="1" title="Create StaticItemVisibilityControls component">
<action>
1. Create `src/components/admin/StaticItemVisibilityControls.tsx` — `'use client'`. Inline visibility editor for a single static row.

2. Props:
```ts
interface Props {
  staticKey: string
  initial: { is_active: boolean; visible_from: string | null; visible_until: string | null }
  onError: (message: string) => void
}
```

3. Behavior:
   - Three inputs: `<input type="checkbox">` for is_active, two `<input type="datetime-local">` for visible_from/visible_until
   - Debounced auto-save: 500ms after last change, call `updateStaticItemVisibility(staticKey, settings)` from `@/lib/actions/staticItemVisibility`
   - Optimistic UI: input value updates immediately; rollback on server error (mirror the `ScreenCycleSettingsAutoSave` rollback pattern)
   - Compact layout — these render inline in a MenuManager row, must not blow up vertical space. Consider a small "Synlighed" inline expander that defaults collapsed, expands on click to show the three inputs

4. Reuse the existing `EyeOff` / `Clock` indicator pattern from MenuManager. If you extract a `VisibilityBadges` component for shared use, place it in the same file or as a small sibling.
</action>
<verify>npx tsc --noEmit 2>&1 | grep -v "^$" | head -20 || echo "tsc clean"</verify>
<done>StaticItemVisibilityControls renders, persists to staticItemVisibility action, and rolls back on error.</done>
</task>

<task id="2" title="Extend ScreenAssignmentToggle to support static items">
<action>
1. Read `src/components/admin/ScreenAssignmentToggle.tsx`. Existing prop signature uses `pageId: string`.

2. Extend to a discriminated union of subjects:
```ts
type AssignmentSubject =
  | { kind: 'page'; pageId: string; pageLabel: string }
  | { kind: 'static'; staticKey: string; staticLabel: string }

interface Props {
  subject: AssignmentSubject
  screens: ScreenInfo[]
  assignedScreenIds: string[]
  onError: (message: string) => void
  disabled?: boolean
}
```

3. Update internal `toggleScreen` to dispatch the right action by `subject.kind`:
   - `'page'` → `addPageToScreen` / `removePageFromScreen` (existing)
   - `'static'` → `addStaticItemToScreen` / `removeStaticItemFromScreen` (new from 08-02)

4. Update internal click/long-press handlers — no behavior change, just the action call differs.

5. Update existing call site in MenuManager (the page-row branch) to pass the new `subject` prop shape: `{ kind: 'page', pageId: page.id, pageLabel: page.title }`.

6. The popover menu rendering does not change — same screen list with checkbox toggles.
</action>
<verify>npx tsc --noEmit 2>&1 | grep -v "^$" | head -20 || echo "tsc clean"</verify>
<done>ScreenAssignmentToggle accepts both subject kinds; existing page-row behavior unchanged.</done>
</task>

<task id="3" title="Wire static rows in MenuManager">
<action>
1. Read `src/components/admin/MenuManager.tsx`. Identify the `kind: 'static'` row branch.

2. The sider admin page now passes additional props to MenuManager:
   - `staticVisibilityMap: Record<string, StaticItemVisibility>` — from `getStaticItemVisibilityMap()`
   - `staticAssignmentsByKey: Record<string, string[]>` — from `getStaticAssignmentsMapByKey()`

3. Update MenuManager's static-row rendering:
   - Show existing chevron + drag handle + icon + label
   - Add visibility-status badges (EyeOff if `!is_active`, Clock if outside window) — reuse the same logic dynamic pages use
   - Add `<StaticItemVisibilityControls staticKey={key} initial={visibility} onError={onError} />` (collapsed by default)
   - Add `<ScreenAssignmentToggle subject={{ kind: 'static', staticKey, staticLabel }} ... />` — but ONLY for the eligible subset (`galleri, deltagere, hvor, tasks, program`). For ineligible keys (`camera, photos, minder`), do NOT render the toggle (or render disabled with title "Ikke tilgængelig på skærm").

   IMPORTANT: confirm with PROJECT.md R25 / ROADMAP Phase 8 success criteria that `minder` is NOT in scope for screen-toggle (per ROADMAP wording, only "Galleri, Deltagere, Hvor, Opgaver, Program" are screen-eligible). If this list is wrong, fix the implementation to match.

4. Replace any existing "Indbygget" badge with the visibility/screen-status indicators when relevant. Keep "Indbygget" only when there's nothing else to show.

5. Verify drag behavior still works after the new controls are added — the dnd-kit drag handle must remain the only drag activator. Buttons inside the row must not start drags (existing 5px activation distance handles this, but visually-active components like checkbox or datetime-local inputs need touch-action or stopPropagation if they leak).
</action>
<verify>npx tsc --noEmit 2>&1 | grep -v "^$" | head -20 || echo "tsc clean"</verify>
<done>Static rows show visibility controls + screen-toggle (where eligible). Drag-and-drop unaffected.</done>
</task>

<task id="4" title="Update sider admin page to fetch new data">
<action>
1. Read `src/app/(admin)/admin/sider/page.tsx`.

2. Add `getStaticItemVisibilityMap` and `getStaticAssignmentsMapByKey` to the parallel `Promise.all([...])` already there.

3. Pass results as new props to `<MenuManager ... />`.

4. No other changes — the page is a server component; reconciliation logic (`getResolvedNavForAdmin`) doesn't change in this plan.
</action>
<verify>npx tsc --noEmit 2>&1 | grep -v "^$" | head -20 || echo "tsc clean"</verify>
<done>Sider page passes both new maps to MenuManager.</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` exits with code 0
- Existing dynamic-page rows render unchanged in MenuManager
- Static rows now show visibility-status badges + inline visibility editor + screen-toggle (where eligible)
- Drag-and-drop reorder still works for both row kinds
- Auto-save on visibility change triggers `updateStaticItemVisibility`
- Screen-toggle on static rows triggers `addStaticItemToScreen` / `removeStaticItemFromScreen`
</verification>

<success_criteria>
- Admin can toggle Galleri's screen-show via the new Monitor button on its row
- Admin can toggle Deltagere's `is_active=false` and a Clock badge appears next to it (after the visibility window check evaluates as outside)
- Kamera and Billeder rows have visibility controls but NO screen-toggle button
- All inline edits debounced-auto-save with optimistic UI + rollback on server error
</success_criteria>

<output>
Write `.planning/phases/08-static-item-visibility-screen-rendering/08-03-SUMMARY.md` with:
- **Status**: Complete | Failed
- **Files modified/created**: absolute paths
- **Eligibility decision recap**: which static keys got screen-toggle (galleri, deltagere, hvor, tasks, program — confirm vs ROADMAP wording)
- **Component split**: did you extract VisibilityBadges? Where do they live now?
- **Drag-and-drop integrity**: confirmation that adding interactive controls didn't break drag (note any touch-action / pointer-event tweaks needed)
- **Auto-save behavior**: debounce delay used, rollback strategy
- **Verification**: tsc result, manual click-test list for the user
</output>
