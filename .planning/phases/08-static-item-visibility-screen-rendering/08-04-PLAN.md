---
phase: 8
plan: 08-04
wave: 2
depends_on: [08-01]
agent: engineering-senior-developer
files_modified:
  - src/lib/actions/settings.ts
  - src/app/[uuid]/galleri/page.tsx
  - src/app/[uuid]/deltagere/page.tsx
  - src/app/[uuid]/hvor/page.tsx
  - src/app/[uuid]/opgaver/page.tsx
  - src/app/[uuid]/minder/page.tsx
  - src/app/[uuid]/billeder/page.tsx
  - src/app/[uuid]/billeder/kamera/page.tsx
  - src/app/[uuid]/program/page.tsx
must_haves:
  - getResolvedNavForGuest filters static items by visibility (is_active + time window)
  - Each static guest route calls notFound() when its static-item visibility evaluates as hidden
  - Existing dynamic-page visibility behavior unchanged
  - All bottom-menu items respect visibility unanimously (a hidden item should not appear in the bottom menu AND its URL should 404)
  - No changes to admin paths, server actions other than settings.ts, or screen render
---

<objective>
Wire static-item visibility into the guest-facing surfaces. The bottom menu hides invisible static items; the routes themselves return 404 when accessed directly with the URL.
</objective>

<context>
- Read 08-CONTEXT.md and 08-01-SUMMARY.md to know how visibility is stored
- Existing `getResolvedNavForGuest(uuid)` in `src/lib/actions/settings.ts` already iterates the merged nav order. For `kind: 'static'` items it currently always emits a nav entry. This plan adds visibility filtering matching the dynamic-page filter already in place
- Existing `isPageVisibleNow` helper in `src/lib/guest/navItems.ts` works for any `{ is_active, visible_from, visible_until }` shape — reuse it for static items by adapting the visibility-map entry shape
- Existing dynamic guest page that uses notFound: `src/app/[uuid]/p/[slug]/page.tsx` — that pattern is the model for the static-route guards
- Static guest routes (in scope for visibility): `/{uuid}/galleri`, `/{uuid}/deltagere`, `/{uuid}/hvor`, `/{uuid}/opgaver`, `/{uuid}/minder`, `/{uuid}/billeder`, `/{uuid}/billeder/kamera`, `/{uuid}/program`. Visibility key mapping:
  - `/galleri` → `galleri`
  - `/deltagere` → `deltagere`
  - `/hvor` → `hvor`
  - `/opgaver` → `tasks`
  - `/minder` → `minder`
  - `/billeder` → `photos`
  - `/billeder/kamera` → `camera`
  - `/program` → `program`
- The forside `/{uuid}` is NOT in scope for visibility — it's the home page and always available
</context>

<tasks>

<task id="1" title="Filter static items in getResolvedNavForGuest">
<action>
1. Read `src/lib/actions/settings.ts` and locate `getResolvedNavForGuest`.

2. Inside, add a parallel fetch of `getStaticItemVisibilityMap()` (from `@/lib/actions/staticItemVisibility`) alongside the existing `app_settings.nav_order` and `pages` queries.

3. In the loop that emits `ResolvedNavGuestItem` entries:
   - For `kind: 'static'`: look up the visibility entry by `item.key`. If the entry exists, evaluate visibility (use `isPageVisibleNow` with the shape adapter). If hidden, skip emitting the item.
   - For `kind: 'page'`: behavior unchanged.

4. Default behavior preserved: a static key with NO entry in the visibility map is treated as fully visible (current behavior).

5. The admin-side `getResolvedNavForAdmin` should NOT filter by visibility — admin must see all items including hidden ones (with badges). So this change ONLY affects the guest resolver.
</action>
<verify>npx tsc --noEmit 2>&1 | grep -v "^$" | head -20 || echo "tsc clean"</verify>
<done>Hidden static items disappear from guest bottom menu while admin still sees them with badges.</done>
</task>

<task id="2" title="Add notFound() guards to all static guest routes">
<action>
For each of the 8 static guest routes listed in context, modify the `page.tsx` to call `notFound()` from `next/navigation` when the static-item is hidden.

Pattern (apply to each):

```tsx
import { notFound } from 'next/navigation'
import { getStaticItemVisibilityMap, isStaticItemVisibleNow } from '@/lib/actions/staticItemVisibility'

// at the top of the page component:
const visibilityMap = await getStaticItemVisibilityMap()
if (!isStaticItemVisibleNow('<KEY>', visibilityMap)) notFound()
```

The `<KEY>` value per route per the mapping table in CONTEXT (galleri, deltagere, hvor, tasks, minder, photos, camera, program).

IMPORTANT: do this AFTER the existing `await resolveGuest()` call so auth happens first. Order:
1. `await resolveGuest()` — sets up guest identity (or throws Unauthorized → caught by error.tsx)
2. visibility check → notFound() if hidden
3. existing page logic continues

Don't fetch the visibility map twice — share it across pages by single-call OR accept the small cost of one extra select per route (each page is independent server-component-rendered, so they can't share state without a request-level cache; just keep the per-page call simple).

Tasks routes: the camera page (`/billeder/kamera`) has no bottom menu (per existing code); still apply the visibility guard.
</action>
<verify>grep -l "isStaticItemVisibleNow\|getStaticItemVisibilityMap" src/app/[uuid] -r | wc -l</verify>
<done>All 8 static guest routes call notFound() when their static-key visibility says hidden.</done>
</task>

<task id="3" title="Verify integration end-to-end">
<action>
1. Run `npx tsc --noEmit` — expect 0 errors.
2. Read each modified guest page and confirm the visibility check is positioned correctly (after auth, before any other DB fetch that's not strictly required).
3. Confirm `getResolvedNavForGuest` in settings.ts has been extended with the visibility fetch and filter; admin resolver remains untouched.
</action>
<verify>npx tsc --noEmit 2>&1 | tail -3</verify>
<done>Type-check clean; visibility integration consistent across all 8 routes.</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` exits 0
- Hidden static keys (toggled via 08-03 admin UI) absent from `getResolvedNavForGuest` output
- Hidden static URLs return 404 when accessed directly
- Admin-side `getResolvedNavForAdmin` continues showing hidden items with badges (NOT filtered)
- Dynamic-page visibility behavior unchanged
</verification>

<success_criteria>
- Toggling Galleri's `is_active=false` in admin: Galleri vanishes from guest bottom menu AND `/{uuid}/galleri` 404s
- Setting Deltagere's `visible_from` to a future date: same effect
- Re-enabling restores both menu visibility and URL access
- Forside (`/{uuid}`) remains accessible regardless of static-item visibility (it's not a static menu item)
</success_criteria>

<output>
Write `.planning/phases/08-static-item-visibility-screen-rendering/08-04-SUMMARY.md` with:
- **Status**: Complete | Failed
- **Files modified**: absolute paths
- **Visibility-key mapping confirmed**: route → static_key table
- **Performance note**: 1 extra DB query per static page render — acceptable for a low-traffic admin app
- **Verification**: tsc result, manual test list for the user
</output>
