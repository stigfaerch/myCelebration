---
phase: 2
plan: 2
wave: 1
slug: valg-indstillinger
title: /admin/deltagere/valg + /admin/indstillinger
depends_on: []
agent: Frontend Developer
must_haves:
  - Choice definitions CRUD (binær, multichoice, tekst)
  - SMS-skabelon kan redigeres og forhåndsvises
status: pending
---

# Plan 02-02: /admin/deltagere/valg + /admin/indstillinger

## Objective
Implementér konfigurerbare deltager-valg (`/admin/deltagere/valg`) og SMS-skabelon indstillinger (`/admin/indstillinger`). Kører parallelt med Plan 02-01 — ingen filoverlapning.

## Context
- `.planning/phases/02-admin-deltagere-information/02-CONTEXT.md`
- `.planning/phases/01-foundation/01-03-SUMMARY.md` — server actions mønster

## Codebase Conventions
- Admin-sider i `src/app/(admin)/admin/` (sidebar layout arves)
- Supabase: `import { supabaseServer } from '@/lib/supabase/server'`
- Server Actions: `'use server'` + `revalidatePath`
- shadcn/ui: button, input, label, separator, sheet, dropdown-menu tilgængeligt
- `lucide-react` tilgængeligt (installeret i Plan 02-01 — hvis Plan 02-01 ikke er kørt endnu, installér her: `npm install lucide-react`)
- Tailwind v4

## Tasks

### Task 1: Choice definitions server actions
**Action**: Opret `src/lib/actions/choices.ts`:

```typescript
'use server'
import { revalidatePath } from 'next/cache'
import { supabaseServer } from '@/lib/supabase/server'

export type ChoiceType = 'binary' | 'multichoice' | 'text'

export async function getChoiceDefinitions() {
  const { data, error } = await supabaseServer
    .from('choice_definitions')
    .select(`
      *,
      guest_choices(count)
    `)
    .order('sort_order')
  if (error) throw new Error(error.message)
  return data
}

export async function createChoiceDefinition(formData: {
  title: string
  type: ChoiceType
  options?: string[]  // for multichoice
  sort_order?: number
}) {
  const payload = {
    ...formData,
    options: formData.options ? formData.options : null,
  }
  const { error } = await supabaseServer.from('choice_definitions').insert(payload)
  if (error) throw new Error(error.message)
  revalidatePath('/admin/deltagere/valg')
}

export async function updateChoiceDefinition(
  id: string,
  formData: { title: string; type: ChoiceType; options?: string[] }
) {
  const { error } = await supabaseServer
    .from('choice_definitions')
    .update({ ...formData, options: formData.options ?? null })
    .eq('id', id)
  if (error) throw new Error(error.message)
  revalidatePath('/admin/deltagere/valg')
}

export async function deleteChoiceDefinition(id: string) {
  const { error } = await supabaseServer
    .from('choice_definitions')
    .delete()
    .eq('id', id)
  if (error) throw new Error(error.message)
  revalidatePath('/admin/deltagere/valg')
}

export async function reorderChoiceDefinitions(ids: string[]) {
  const updates = ids.map((id, index) =>
    supabaseServer
      .from('choice_definitions')
      .update({ sort_order: index })
      .eq('id', id)
  )
  await Promise.all(updates)
  revalidatePath('/admin/deltagere/valg')
}
```

**Verify**: `npx tsc --noEmit` — ingen fejl.

**Done**: Choice definition server actions kompilerer.

---

### Task 2: Valg liste + opret/rediger UI
**Action**: Opret `src/app/(admin)/admin/deltagere/valg/page.tsx` (Server Component):
- Henter alle choice definitions med antal svar
- Viser liste: titel, type (badge), antal svar, handlinger (rediger, slet)
- "Tilføj valg"-knap åbner Sheet (shadcn) med formular

Opret `src/components/admin/ChoiceForm.tsx` (Client Component) med:
- **Titel** (text, required)
- **Type** (select: binær/multichoice/tekst):
  - `binary`: ingen ekstra felter — svaret er "ja" eller "nej"
  - `multichoice`: dynamisk liste af svarmuligheder (tilføj/fjern option med + og × knapper)
  - `text`: ingen ekstra felter — gæsten skriver fritekst
- Submit-knap: "Gem"

Type-labels på dansk:
- `binary` → "Ja/Nej"
- `multichoice` → "Valgmuligheder"
- `text` → "Fritekst"

Slet: brug confirm-dialog. Vis advarsel hvis der allerede er svar på valget: "Der er {N} svar på dette valg. Slet alligevel?"

Rediger: åbn Sheet med eksisterende data præudfyldt.

**Verify**: Valg-liste renderes. Formular vises korrekt per type.

**Done**: Choice definitions kan oprettes, redigeres og slettes.

---

### Task 3: SMS-skabelon indstillinger
**Action**: Opret `src/lib/actions/settings.ts`:

```typescript
'use server'
import { revalidatePath } from 'next/cache'
import { supabaseServer } from '@/lib/supabase/server'

export async function getAppSettings() {
  const { data, error } = await supabaseServer
    .from('app_settings')
    .select('*')
    .single()
  if (error) throw new Error(error.message)
  return data
}

export async function updateSmsTemplate(template: string) {
  const { data: existing } = await supabaseServer
    .from('app_settings')
    .select('id')
    .single()

  if (!existing) throw new Error('App settings not found')

  const { error } = await supabaseServer
    .from('app_settings')
    .update({ sms_template: template })
    .eq('id', existing.id)

  if (error) throw new Error(error.message)
  revalidatePath('/admin/indstillinger')
}
```

Opret `src/app/(admin)/admin/indstillinger/page.tsx`:
- Henter eksisterende SMS-skabelon
- Textarea til at redigere skabelonen
- Forklaring: "Brug `{navn}` for deltagerens navn og `{url}` for det personlige link"
- **Live preview**: viser skabelonen med eksempeldata (navn: "Anders Hansen", url: "https://fest.dk/abc123-...")
  - Previewet opdateres mens admin skriver (Client Component)
- Gem-knap kalder `updateSmsTemplate` Server Action

Opret `src/components/admin/SmsTemplateEditor.tsx` (Client Component) der håndterer live preview.

**Verify**: Skabelon kan gemmes og hentes. Preview opdateres korrekt.

**Done**: SMS-skabelon kan redigeres med live preview.

## Output
- `src/lib/actions/choices.ts`
- `src/lib/actions/settings.ts`
- `src/app/(admin)/admin/deltagere/valg/page.tsx`
- `src/app/(admin)/admin/indstillinger/page.tsx`
- `src/components/admin/ChoiceForm.tsx`
- `src/components/admin/SmsTemplateEditor.tsx`

## Success Criteria
- [ ] Choice definitions liste viser type og antal svar
- [ ] Binær, multichoice og tekst-valg kan oprettes korrekt
- [ ] Multichoice viser dynamisk options-editor
- [ ] Slet viser advarsel ved eksisterende svar
- [ ] SMS-skabelon gemmes til database
- [ ] Live preview erstatter `{navn}` og `{url}` korrekt
- [ ] TypeScript kompilerer rent
