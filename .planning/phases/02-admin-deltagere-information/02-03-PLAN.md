---
phase: 2
plan: 3
wave: 2
slug: information
title: /admin/information — TipTap, festinfo, begivenheder
depends_on: ["02-01", "02-02"]
agent: Senior Developer
must_haves:
  - TipTap RTE-komponent fungerer i admin
  - Festinfo (beskrivelse + invitation upload) kan gemmes
  - Begivenheder kan oprettes, redigeres, slettes
  - Begivenhed-lokationer kan oprettes og slettes
status: pending
---

# Plan 02-03: /admin/information — TipTap, festinfo, begivenheder

## Objective
Installér TipTap og opret delt RTE-komponent. Implementér `/admin/information` med festbeskrivelse (RTE), invitation-upload til Supabase Storage og fuld CRUD for begivenheder med nested lokationer.

## Context
- `.planning/phases/02-admin-deltagere-information/02-CONTEXT.md`
- `.planning/phases/01-foundation/01-02-SUMMARY.md` — Supabase Storage konfiguration
- `.planning/phases/01-foundation/01-03-SUMMARY.md` — server actions mønster

## Codebase Conventions
- Admin-sider i `src/app/(admin)/admin/`
- Supabase service role: `import { supabaseServer } from '@/lib/supabase/server'`
- Supabase anon key til Storage upload fra browser: `import { supabaseClient } from '@/lib/supabase/client'`
- Server Actions med `'use server'` + `revalidatePath`
- `lucide-react` tilgængeligt (installeret i Plan 02-01)
- Tailwind v4, shadcn/ui
- Next.js 16: `revalidatePath` bruges til cache-invalidering efter mutations

## Tasks

### Task 1: TipTap installation + delt RTE-komponent
**Action**:
1. Installér TipTap: `npm install @tiptap/react @tiptap/pm @tiptap/starter-kit`
2. Opret `src/components/admin/RichTextEditor.tsx` (Client Component):

```tsx
'use client'
import { useEditor, EditorContent } from '@tiptap/react'
import StarterKit from '@tiptap/starter-kit'
import { useEffect } from 'react'

interface Props {
  value?: Record<string, unknown> | null  // TipTap JSON
  onChange?: (json: Record<string, unknown>) => void
  placeholder?: string
}

export function RichTextEditor({ value, onChange, placeholder }: Props) {
  const editor = useEditor({
    extensions: [StarterKit],
    content: value ?? '',
    editorProps: {
      attributes: {
        class: 'prose prose-sm max-w-none min-h-[120px] p-3 focus:outline-none',
      },
    },
    onUpdate: ({ editor }) => {
      onChange?.(editor.getJSON() as Record<string, unknown>)
    },
  })

  // Sync external value changes
  useEffect(() => {
    if (editor && value && JSON.stringify(editor.getJSON()) !== JSON.stringify(value)) {
      editor.commands.setContent(value)
    }
  }, [editor, value])

  return (
    <div className="rounded-md border border-input bg-background">
      {/* Toolbar */}
      <div className="flex gap-1 border-b p-1">
        <button
          type="button"
          onClick={() => editor?.chain().focus().toggleBold().run()}
          className={`rounded px-2 py-1 text-xs ${editor?.isActive('bold') ? 'bg-accent' : 'hover:bg-accent'}`}
        >
          B
        </button>
        <button
          type="button"
          onClick={() => editor?.chain().focus().toggleItalic().run()}
          className={`rounded px-2 py-1 text-xs italic ${editor?.isActive('italic') ? 'bg-accent' : 'hover:bg-accent'}`}
        >
          I
        </button>
        <button
          type="button"
          onClick={() => editor?.chain().focus().toggleBulletList().run()}
          className={`rounded px-2 py-1 text-xs ${editor?.isActive('bulletList') ? 'bg-accent' : 'hover:bg-accent'}`}
        >
          •
        </button>
        <button
          type="button"
          onClick={() => editor?.chain().focus().toggleOrderedList().run()}
          className={`rounded px-2 py-1 text-xs ${editor?.isActive('orderedList') ? 'bg-accent' : 'hover:bg-accent'}`}
        >
          1.
        </button>
      </div>
      <EditorContent editor={editor} />
    </div>
  )
}
```

3. Opret `src/components/admin/RichTextDisplay.tsx` (Server Component) til at vise TipTap JSON som HTML i read-only visning:

```tsx
import { generateHTML } from '@tiptap/html'
import StarterKit from '@tiptap/starter-kit'

interface Props {
  content: Record<string, unknown> | null
}

export function RichTextDisplay({ content }: Props) {
  if (!content) return null
  const html = generateHTML(content as Parameters<typeof generateHTML>[0], [StarterKit])
  return (
    <div
      className="prose prose-sm max-w-none"
      dangerouslySetInnerHTML={{ __html: html }}
    />
  )
}
```

**Verify**: `npx tsc --noEmit` — ingen fejl. `@tiptap/react` importerer uden fejl.

**Done**: TipTap installeret og RTE-komponent klar til brug.

---

### Task 2: Festinfo server actions + side (beskrivelse + invitation upload)
**Action**: Opret `src/lib/actions/information.ts`:

```typescript
'use server'
import { revalidatePath } from 'next/cache'
import { supabaseServer } from '@/lib/supabase/server'

export async function getFestInfo() {
  const { data, error } = await supabaseServer
    .from('fest_info')
    .select('*')
    .single()
  if (error) throw new Error(error.message)
  return data
}

export async function updateFestDescription(description: Record<string, unknown>) {
  const { data: existing } = await supabaseServer.from('fest_info').select('id').single()
  if (!existing) throw new Error('fest_info not found')
  const { error } = await supabaseServer
    .from('fest_info')
    .update({ description })
    .eq('id', existing.id)
  if (error) throw new Error(error.message)
  revalidatePath('/admin/information')
}

export async function updateInvitationUrl(url: string) {
  const { data: existing } = await supabaseServer.from('fest_info').select('id').single()
  if (!existing) throw new Error('fest_info not found')
  const { error } = await supabaseServer
    .from('fest_info')
    .update({ invitation_url: url })
    .eq('id', existing.id)
  if (error) throw new Error(error.message)
  revalidatePath('/admin/information')
}
```

Opret `src/app/(admin)/admin/information/page.tsx` som Server Component med to sektioner:

**Sektion 1 — Festbeskrivelse:**
- Viser nuværende TipTap JSON via `RichTextEditor`
- Gem-knap kalder `updateFestDescription` Server Action

**Sektion 2 — Invitation upload:**
- Vis nuværende invitation URL (link + thumbnail hvis billede)
- Upload-knap åbner fil-input (accepterer PDF, PNG, JPG)
- Upload håndteres client-side til Supabase Storage bucket `invitations` (public bucket — skal oprettes manuelt i Supabase Dashboard):
  - Brug `supabaseClient` (anon key) fra `src/lib/supabase/client.ts`
  - Upload til `invitations/{filename}` og få public URL
  - Kald `updateInvitationUrl(publicUrl)` Server Action med den returnerede URL
- Opret `src/components/admin/InvitationUpload.tsx` som Client Component til at håndtere upload

Dokumentér i kommentaren at `invitations` bucket skal oprettes i Supabase Dashboard → Storage → New bucket (public: true).

**Verify**: Festbeskrivelse kan redigeres og gemmes. Upload-komponent renderes.

**Done**: Festinfo-sektion er funktionel.

---

### Task 3: Begivenheder CRUD + nested lokationer
**Action**: Tilføj event-actions til `src/lib/actions/information.ts`:

```typescript
export async function getEvents() {
  const { data, error } = await supabaseServer
    .from('events')
    .select(`*, event_locations(*)`)
    .order('sort_order')
  if (error) throw new Error(error.message)
  return data
}

export async function createEvent(formData: {
  name: string
  description?: string
  start_time?: string
  address?: string
  google_maps_embed?: string
  map_image_url?: string
  map_image_description?: string
}) {
  const { error } = await supabaseServer.from('events').insert(formData)
  if (error) throw new Error(error.message)
  revalidatePath('/admin/information')
}

export async function updateEvent(id: string, formData: Omit<Parameters<typeof createEvent>[0], never>) {
  const { error } = await supabaseServer.from('events').update(formData).eq('id', id)
  if (error) throw new Error(error.message)
  revalidatePath('/admin/information')
}

export async function deleteEvent(id: string) {
  const { error } = await supabaseServer.from('events').delete().eq('id', id)
  if (error) throw new Error(error.message)
  revalidatePath('/admin/information')
}

export async function createEventLocation(eventId: string, formData: { title: string; description?: string }) {
  const { error } = await supabaseServer
    .from('event_locations')
    .insert({ ...formData, event_id: eventId })
  if (error) throw new Error(error.message)
  revalidatePath('/admin/information')
}

export async function deleteEventLocation(id: string) {
  const { error } = await supabaseServer.from('event_locations').delete().eq('id', id)
  if (error) throw new Error(error.message)
  revalidatePath('/admin/information')
}
```

Tilføj "Begivenheder"-sektion til `/admin/information/page.tsx`:
- Liste over begivenheder med expand/collapse (en begivenhed ad gangen)
- Per begivenhed: navn, starttid, adresse
- "Ny begivenhed"-knap åbner Sheet/dialog
- Begivenhed-formular (Client Component `src/components/admin/EventForm.tsx`):
  - Navn (required)
  - Starttid (datetime-local input)
  - Adresse (text)
  - Google Maps embed-kode (textarea — admin indsætter `<iframe>`-kode fra Google Maps)
  - Kortbillede upload (til Supabase Storage bucket `maps`, dokumentér at det skal oprettes)
  - Kortbillede beskrivelse
- Nested lokationer under hver begivenhed:
  - Liste: titel, beskrivelse, slet-knap
  - "Tilføj lokation"-inline formular (titel + beskrivelse)

**Verify**: Begivenhed kan oprettes, redigeres, slettes. Lokation kan tilføjes og fjernes.

**Done**: Fuld CRUD for begivenheder og lokationer.

## Output
- `package.json` opdateret med TipTap deps
- `src/components/admin/RichTextEditor.tsx`
- `src/components/admin/RichTextDisplay.tsx`
- `src/components/admin/InvitationUpload.tsx`
- `src/components/admin/EventForm.tsx`
- `src/lib/actions/information.ts`
- `src/app/(admin)/admin/information/page.tsx`

## Manual Action Required (Supabase Storage)
Opret følgende Storage buckets i Supabase Dashboard → Storage:
1. `invitations` — public: true (invitation PDF/billede)
2. `maps` — public: true (kortbilleder til begivenheder)

## Success Criteria
- [ ] TipTap editor vises og gemmer JSON til `fest_info.description`
- [ ] Invitation kan uploades (PDF/PNG/JPG) og URL gemmes
- [ ] Begivenhed kan oprettes med alle felter
- [ ] Begivenhed kan redigeres og slettes
- [ ] Lokationer kan tilføjes og fjernes under begivenhed
- [ ] TypeScript kompilerer rent (`npx tsc --noEmit`)
