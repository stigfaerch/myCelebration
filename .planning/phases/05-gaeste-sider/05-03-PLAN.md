---
phase: 5
plan: 05-03
wave: 2
depends_on: [05-01]
agent: engineering-senior-developer
files_modified:
  - src/lib/storage/upload.ts
  - src/app/[uuid]/billeder/page.tsx
  - src/app/[uuid]/billeder/kamera/page.tsx
  - src/app/[uuid]/minder/page.tsx
  - src/app/[uuid]/galleri/page.tsx
  - src/lib/actions/guest/photos.ts
  - src/lib/actions/guest/memories.ts
  - src/components/guest/Camera.tsx
  - src/components/guest/PhotoGrid.tsx
  - src/components/guest/MemoryManager.tsx
  - src/components/guest/MemoryForm.tsx
  - src/components/guest/GalleryHorizontal.tsx
  - src/components/guest/GalleryVertical.tsx
must_haves:
  - Image upload helper that stores original in Supabase Storage, returns public URL
  - Camera route captures + uploads + inserts photo row
  - Camera route has NO bottom-menu (route-level override)
  - Photo grid shows own photos with delete
  - Memory CRUD with optional image upload/delete
  - Gallery (horizontal primary + vertical basic) reads gallery_config and filters photos/memories
---

<objective>
Build the guest-side media features: image upload pipeline, camera capture, own-photos grid, memories CRUD (with image upload), and the public gallery. The camera route is the only guest route WITHOUT a bottom-menu — it's full-screen. Memories and photos upload originals to Supabase Storage (no async derivative pipeline in v1 — that's a Phase 5 stretch deferred). Gallery rendering is horizontal-first (mobile landscape / kiosk screen) with a basic vertical fallback.
</objective>

<context>
- Read 05-CONTEXT.md for full phase context
- Read 05-01-SUMMARY.md for resolveGuest import and camera bottom-menu opt-out decision
- Phase 4 created admin views over `photos`, `memories`, `gallery_config` — DO NOT modify those action files; create separate guest actions in `src/lib/actions/guest/`
- Phase 4 established `normalizeGuest` helper pattern — reuse
- Phase 4 established `clearScreenOverridesFor('photo' | 'memory', id)` — guest delete operations MUST call this too (a screen may be showing the guest's photo when the guest deletes it)
- Image handling per `.planning/IMAGE_HANDLING_AND_STORAGE.md`:
  - Originals in `/images/original/{image_id}.{ext}`
  - Derivatives (medium/thumb) deferred — render originals via `<img>` with CSS sizing
  - Upload via Supabase Storage bucket `images` (create bucket manually if not exists — add to STATE.md Manual Actions)
- shadcn/ui + lucide-react + Tailwind v4
- Server Actions cannot receive File objects directly — use FormData API (`formData.get('file')` in server action returning File) — Next.js supports this in Server Actions
- Camera uses `navigator.mediaDevices.getUserMedia({ video: true })` + `<canvas>` capture + upload via FormData
- Gallery config: source ('photos' | 'memories' | 'both'), interval_seconds (int), display_type ('single' | 'quad' | 'frames'), show_memory_text (bool), filter_after/before (timestamptz)
</context>

<tasks>

<task id="1" title="Storage upload helper + guest photos actions + /{uuid}/billeder + camera">
<action>
1. Create `src/lib/storage/upload.ts`:
   ```ts
   'use server'
   import { supabaseServer } from '@/lib/supabase/server'

   const ALLOWED = new Set(['image/jpeg', 'image/png', 'image/heic'])
   const MAX_BYTES = 10 * 1024 * 1024 // 10 MB

   export interface UploadResult {
     storage_url: string
     path: string
   }

   export async function uploadImage(file: File, opts: { prefix: 'photo' | 'memory' }): Promise<UploadResult> {
     if (!ALLOWED.has(file.type)) throw new Error('Filtype ikke understøttet')
     if (file.size > MAX_BYTES) throw new Error('Filen er for stor (maks 10 MB)')

     const ext = (file.name.split('.').pop() || 'jpg').toLowerCase()
     const id = crypto.randomUUID()
     const path = `original/${opts.prefix}-${id}.${ext}`

     const { error } = await supabaseServer.storage
       .from('images')
       .upload(path, file, { contentType: file.type, upsert: false })
     if (error) throw new Error('Upload fejlede')

     const { data } = supabaseServer.storage.from('images').getPublicUrl(path)
     return { storage_url: data.publicUrl, path }
   }
   ```
   - Note: HEIC upload works but many browsers don't render HEIC natively. Accept upload but document Phase 5 limitation (most phones convert to JPEG in the capture flow anyway).

2. Create `src/lib/actions/guest/photos.ts`:
   - `getMyPhotos()` — select photos where guest_id = resolveGuest().id, order by taken_at desc
   - `createPhotoFromFile(formData: FormData)`:
     - `const guest = await resolveGuest()`
     - Reject if guest.type === 'screen' — throw 'Not available for screens'
     - Extract file via `formData.get('file') as File | null`; validate non-null
     - Call `uploadImage(file, { prefix: 'photo' })`
     - Insert row: `{ guest_id, storage_url, taken_at: now(), is_active: true }`
     - revalidatePath `/${guest.id}/billeder`
   - `deleteMyPhoto(id)`:
     - resolveGuest
     - Fetch photo row; verify guest_id matches
     - Call `clearScreenOverridesFor('photo', id)` (import from `@/lib/actions/screen`)
     - Optionally delete storage object: `supabaseServer.storage.from('images').remove([path])` — derive path from storage_url or store path column separately
     - Delete DB row
     - revalidatePath
   - Decision: parse the `path` suffix from `storage_url` to delete the storage object. Document this as fragile in SUMMARY — consider adding a `storage_path` column in Phase 5 stretch or Phase 7 hardening.

3. Create `src/components/guest/PhotoGrid.tsx` (client):
   - Props: `{ initialPhotos, uuid }`
   - State: local `photos` with useEffect prop sync (Phase 4 pattern)
   - Grid: 3 columns mobile, `<img>` thumbnails with aspect-square object-cover
   - Tap photo → lightbox overlay (full-width) with Delete button
   - Delete: `window.confirm` + `deleteMyPhoto(id)` + optimistic filter + router.refresh
   - Empty state: "Du har ikke taget nogen billeder endnu. Gå til kamera."
   - FAB "Tag billede" link to `/{uuid}/billeder/kamera`

4. Create `src/app/[uuid]/billeder/page.tsx`:
   - Server Component, fetch `getMyPhotos()`, render PhotoGrid

5. Create `src/components/guest/Camera.tsx` (client):
   - getUserMedia facing environment (back camera preferred: `{ video: { facingMode: 'environment' } }`)
   - Render `<video>` preview + round capture button
   - Capture: draw video frame to canvas, canvas.toBlob(→ File) with quality 0.85, POST via FormData to `createPhotoFromFile`
   - After success: show thumb + "Gå til billeder" link (back navigation)
   - Error states: permission denied, device not available, upload failed
   - Stop the stream on unmount

6. Create `src/app/[uuid]/billeder/kamera/page.tsx`:
   - Server Component, calls `assertNotScreen()` (throws for screen-type)
   - Renders `<Camera uuid={uuid} />` wrapped in a fullscreen div
   - **Bottom-menu opt-out**: this route must render WITHOUT the bottom-menu. Since `layout.tsx` is higher up the tree and Server Components can't easily branch on route, use ONE of these:
     - Option A: put the page inside a route group that has its own layout (e.g., `src/app/[uuid]/billeder/kamera/` already exists — add a `layout.tsx` that simply renders `{children}` and use CSS `position: fixed; inset: 0; z-index: 50` on the camera container to visually cover the layout's bottom-menu. This is the pragmatic choice.
     - Option B: pass `showBottomMenu` through context — complex for Server Components
   - Use Option A: camera page renders a full-viewport fixed overlay that visually covers the bottom-menu.
</action>
<verify>npx tsc --noEmit</verify>
<done>Upload helper works; /{uuid}/billeder renders own photos; camera page captures and uploads; camera visually covers bottom-menu; tsc clean</done>
</task>

<task id="2" title="Guest memories CRUD + /{uuid}/minder">
<action>
1. Create `src/lib/actions/guest/memories.ts`:
   - `getMyMemories()` — memories where guest_id = resolveGuest().id, order by created_at desc
   - `createMemory(formData: FormData)`:
     - resolveGuest; reject if type === 'screen'
     - Extract fields: title (required), type (enum), description?, when_date?, file? (optional image)
     - If file present: uploadImage(file, { prefix: 'memory' }) → image_url
     - Insert row
     - revalidatePath `/${guest.id}/minder`
   - `updateMemory(id, formData: FormData)`:
     - resolveGuest; fetch memory, verify guest_id matches
     - Update title/type/description/when_date
     - If new file present: upload and replace image_url (old storage object left in place; acceptable for v1)
     - If `removeImage=true` form field: set image_url=null (and best-effort delete old storage object)
   - `deleteMyMemory(id)`:
     - resolveGuest; verify ownership
     - `clearScreenOverridesFor('memory', id)`
     - Best-effort delete storage object if image_url present
     - Delete DB row

2. Create `src/components/guest/MemoryForm.tsx` (client):
   - Props: `{ memory?: Memory | null, onSave, onCancel }`
   - Fields: title, type (select with Sjov/Højtidelig/Hverdag/Milepæl), description (textarea), when_date (text input — free-form like "Sommeren 2018"), image (file input + preview + "Fjern billede" toggle if existing image)
   - Submit: build FormData and call createMemory or updateMemory
   - useTransition, error display

3. Create `src/components/guest/MemoryManager.tsx` (client):
   - Props: `{ initialMemories }`
   - List own memories with edit (opens form) + delete (confirm)
   - "Nyt minde" button opens form
   - Reuse Phase 4 type labels/colors: funny→Sjov (yellow), solemn→Højtidelig (purple), everyday→Hverdag (blue), milestone→Milepæl (green)
   - useEffect prop sync, router.refresh after mutations, optimistic filter on delete

4. Create `src/app/[uuid]/minder/page.tsx`:
   - Server Component, fetch `getMyMemories()`, render MemoryManager
</action>
<verify>npx tsc --noEmit</verify>
<done>Guest can CRUD memories including optional image upload/replace/remove. tsc clean.</done>
</task>

<task id="3" title="/{uuid}/galleri — horizontal + vertical gallery">
<action>
1. Create `src/lib/actions/guest/gallery.ts` (new — guest read of gallery config + items):
   - `getGalleryItems()`:
     - Read gallery_config singleton (reuse admin ensureGalleryConfig? NO — that creates rows if missing. Here, guest-side should just read. Duplicate minimal read-only getter to avoid cross-module auth coupling — or just call `getGalleryConfig` from `@/lib/actions/gallery` which does not require admin. Check if it requires assertAdmin — Phase 4 getter does not. Good, reuse.)
     - Based on source ('photos' | 'memories' | 'both'):
       - photos: fetch photos where is_active=true, applying filter_after/before on taken_at
       - memories: fetch memories applying filter_after/before on created_at
     - Merge into a common shape: `{ kind: 'photo' | 'memory', id, image_url, title?, description?, timestamp }[]`
     - Sort by timestamp desc
   - Returns `{ config, items }` tuple

2. Create `src/components/guest/GalleryHorizontal.tsx` (client — primary design):
   - Props: `{ config, items }`
   - Auto-rotation with `config.interval_seconds`. Use `setInterval` cleared on unmount
   - Render based on `display_type`:
     - `single`: one item fills viewport (16:9 aspect, object-cover)
     - `quad`: 2×2 grid of four recent items
     - `frames`: 3-frame layout (center large, two side smaller) — flex with specific ratios
   - For memories: show title + description iff `config.show_memory_text`
   - Smooth crossfade transition (opacity + transform)
   - Pause on hover (desktop) or on tap (mobile)

3. Create `src/components/guest/GalleryVertical.tsx` (client — basic):
   - Props: `{ items, config }`
   - Simple vertical scroll list, each item `aspect-square` with title overlay if memory + show_memory_text
   - No auto-rotation

4. Create `src/app/[uuid]/galleri/page.tsx`:
   - Server Component
   - Fetch config + items
   - Detect orientation server-side via `headers()` user-agent? Complex and unreliable. Simpler: render BOTH components, toggle with Tailwind CSS (`hidden landscape:block` vs `block landscape:hidden`)
   - Decision: use Tailwind's `landscape:` / `portrait:` variants. Horizontal for landscape; vertical for portrait
   ```tsx
   <div className="w-full h-[calc(100vh-4rem-4rem)]">
     <div className="hidden landscape:block h-full"><GalleryHorizontal ... /></div>
     <div className="block landscape:hidden h-full"><GalleryVertical ... /></div>
   </div>
   ```
   - For screen-type (always landscape kiosk), this resolves to horizontal — matches plan intent

5. Decision notes in SUMMARY:
   - No admin-controlled "standard deaktiveret for alm. deltagere" gate yet. R22 says "standard deaktiveret for alm. deltagere." In v1 we render gallery to all non-screen guests. Admin visibility toggle per page is Phase 4 scope (pages table) — extending that to gallery is a future feature. Flag this as a potential Phase 6/7 enhancement.
</action>
<verify>npx tsc --noEmit</verify>
<done>/{uuid}/galleri renders horizontal in landscape and vertical in portrait, with auto-rotation in horizontal mode. tsc clean.</done>
</task>

</tasks>

<verification>
```bash
npx tsc --noEmit
```
Zero errors expected.

Manual spot-check:
- Visit `/{uuid}/billeder` — shows own photos grid; delete works
- Visit `/{uuid}/billeder/kamera` — bottom-menu is visually covered; capture + upload round-trips back to /billeder
- Visit `/{uuid}/minder` — create with image; edit; delete
- Visit `/{uuid}/galleri` — horizontal layout in landscape; interval-based rotation; memory text toggles via config
- Screen-type: visits to `/billeder/kamera` throw (server-side assertNotScreen); `/minder` and `/billeder` are technically accessible but show empty for screen guests (documented, acceptable)
</verification>

<success_criteria>
- Storage bucket `images` configured (if missing: STATE.md records a new manual action)
- uploadImage helper enforces size + type limits, generates unique paths
- /{uuid}/billeder: own photo grid + delete
- /{uuid}/billeder/kamera: camera capture + upload, fullscreen (no bottom-menu visible)
- /{uuid}/minder: full CRUD with optional image
- /{uuid}/galleri: horizontal landscape + vertical portrait, auto-rotation honors interval_seconds
- All mutations enforce resolveGuest + screen rejection
- Delete operations clear matching screen_state overrides via clearScreenOverridesFor
- Zero tsc errors
</success_criteria>

<output>
After execution, write `05-03-SUMMARY.md` with files created/modified, verification results, decisions (especially storage bucket setup, camera bottom-menu opt-out strategy, deferred derivative pipeline, gallery visibility gating), issues (HEIC rendering caveats), and handoff notes for Phase 6 realtime gallery + screen_state live updates.

Add to STATE.md under Manual Actions Required (if not already present):
- "Opret bucket `images` i Supabase Dashboard → Storage (public: true)"
</output>
