---
phase: 3
plan: 2
wave: 2
slug: program
title: /admin/program — nesting, ordering, indslag-tilknytning
depends_on: ["03-01"]
agent: Senior Developer
must_haves:
  - Programpunkter kan oprettes, redigeres, slettes
  - Nesting via parent_id fungerer (ét niveau)
  - Rækkefølge kan flyttes op/ned
  - Indslag (performance) kan tilknyttes et programpunkt
status: pending
---

# Plan 03-02: /admin/program — nesting, ordering, indslag-tilknytning

## Objective
Implementér `/admin/program` med fuld CRUD for programpunkter. Understøtter ét niveau af nesting (parent_id), flytning af rækkefølge (op/ned-knapper på sort_order), og tilknytning af et gæste-indslag (performance) til et programpunkt af type `performance`.

## Context
- `.planning/phases/03-admin-program-indslag-opgaver/03-CONTEXT.md`
- `.planning/phases/03-admin-program-indslag-opgaver/03-01-SUMMARY.md` — `performances.ts` og `tasks.ts` eksisterer nu
- `.planning/phases/02-admin-deltagere-information/02-03-SUMMARY.md` — TipTap og RTE-komponenter tilgængeligt

## Codebase Conventions
- Admin-sider i `src/app/(admin)/admin/`
- Supabase: `import { supabaseServer } from '@/lib/supabase/server'`
- Server Actions: `'use server'` + `revalidatePath`
- shadcn/ui med Base UI — ingen `asChild`; brug `onClick` handlers
- `lucide-react` tilgængeligt (ChevronUp, ChevronDown, Plus, Trash2, Pencil)
- Tailwind v4, TypeScript strict
- `getPerformances` importeres fra `@/lib/actions/performances` (oprettet i Plan 03-01)

## Tasks

### Task 1: Program server actions + /admin/program basis-side

**Action**: Opret `src/lib/actions/program.ts`:

```typescript
'use server'
import { revalidatePath } from 'next/cache'
import { supabaseServer } from '@/lib/supabase/server'

export type ProgramItemType = 'break' | 'performance' | 'info' | 'ceremony'

export interface ProgramItem {
  id: string
  title: string
  start_time: string | null
  duration_minutes: number | null
  type: ProgramItemType
  performance_id: string | null
  sort_order: number
  parent_id: string | null
  notes: string | null
  created_at: string
}

export async function getProgramItems() {
  const { data, error } = await supabaseServer
    .from('program_items')
    .select('*, performances(id, title, type, guest_id, duration_minutes, guests(name))')
    .order('sort_order')
  if (error) throw new Error(error.message)
  return data
}

export async function createProgramItem(formData: {
  title: string
  type: ProgramItemType
  start_time?: string
  duration_minutes?: number | null
  performance_id?: string | null
  parent_id?: string | null
  notes?: string
}) {
  // Place new item last among siblings
  const { count } = await supabaseServer
    .from('program_items')
    .select('*', { count: 'exact', head: true })
    .eq('parent_id', formData.parent_id ?? null)
  const sort_order = count ?? 0

  const { error } = await supabaseServer
    .from('program_items')
    .insert({ ...formData, sort_order })
  if (error) throw new Error(error.message)
  revalidatePath('/admin/program')
}

export async function updateProgramItem(id: string, formData: {
  title: string
  type: ProgramItemType
  start_time?: string
  duration_minutes?: number | null
  performance_id?: string | null
  parent_id?: string | null
  notes?: string
}) {
  const { error } = await supabaseServer
    .from('program_items')
    .update(formData)
    .eq('id', id)
  if (error) throw new Error(error.message)
  revalidatePath('/admin/program')
}

export async function deleteProgramItem(id: string) {
  // Children (parent_id = id) will cascade via DB FK or must be moved first
  // Simple approach: delete children first, then parent
  await supabaseServer.from('program_items').delete().eq('parent_id', id)
  const { error } = await supabaseServer.from('program_items').delete().eq('id', id)
  if (error) throw new Error(error.message)
  revalidatePath('/admin/program')
}
```

Opret `src/app/(admin)/admin/program/page.tsx` (Server Component):
- Henter `items = await getProgramItems()` og `performances = await getPerformances()` (fra `@/lib/actions/performances`)
- Øverst: "Nyt programpunkt"-knap åbner Sheet/dialog med `ProgramItemForm`
- Sender `items` og `performances` til Client Component `ProgramManager`

**Verify**: `npx tsc --noEmit` — ingen fejl.

**Done**: `/admin/program` renderes med data. Server actions kompilerer.

---

### Task 2: Rækkefølgeflytning (sort_order) + nesting (parent_id)

**Action**: Tilføj til `src/lib/actions/program.ts`:

```typescript
export async function moveProgramItemUp(id: string) {
  // Find current item and its sibling above (same parent_id, lower sort_order)
  const { data: current } = await supabaseServer
    .from('program_items')
    .select('sort_order, parent_id')
    .eq('id', id)
    .single()
  if (!current) return

  const { data: above } = await supabaseServer
    .from('program_items')
    .select('id, sort_order')
    .eq('parent_id', current.parent_id ?? null)
    .lt('sort_order', current.sort_order)
    .order('sort_order', { ascending: false })
    .limit(1)
    .single()
  if (!above) return // Already at top

  // Swap sort_orders
  await Promise.all([
    supabaseServer.from('program_items').update({ sort_order: above.sort_order }).eq('id', id),
    supabaseServer.from('program_items').update({ sort_order: current.sort_order }).eq('id', above.id),
  ])
  revalidatePath('/admin/program')
}

export async function moveProgramItemDown(id: string) {
  const { data: current } = await supabaseServer
    .from('program_items')
    .select('sort_order, parent_id')
    .eq('id', id)
    .single()
  if (!current) return

  const { data: below } = await supabaseServer
    .from('program_items')
    .select('id, sort_order')
    .eq('parent_id', current.parent_id ?? null)
    .gt('sort_order', current.sort_order)
    .order('sort_order', { ascending: true })
    .limit(1)
    .single()
  if (!below) return // Already at bottom

  await Promise.all([
    supabaseServer.from('program_items').update({ sort_order: below.sort_order }).eq('id', id),
    supabaseServer.from('program_items').update({ sort_order: current.sort_order }).eq('id', below.id),
  ])
  revalidatePath('/admin/program')
}
```

Opret `src/components/admin/ProgramManager.tsx` (Client Component):
- Props: `items: ProgramItem[]` (med nested performances + guests), `performances: Performance[]`
- Bygger to-niveau træ: top-niveau items (parent_id = null) + børn grupperet under forælder
- Render-rækkefølge: top-niveau items i sort_order; under hvert: børn i sort_order
- Per item:
  - Venstre: ▲/▼ knapper (`moveProgramItemUp`/`moveProgramItemDown`)
  - Titel, type-badge, starttid, varighed
  - Tilknyttet indslag (hvis performance_id: vis indslag-titel)
  - Rediger-knap, slet-knap
  - "Tilføj underordnet punkt"-knap (sætter `parent_id` i ny-item dialog)
- Top-niveau items viser ▶ til at kollapse/ekspandere børn

**Verify**: `npx tsc --noEmit` — ingen fejl.

**Done**: Op/ned-flytning og nesting vises korrekt i UI.

---

### Task 3: ProgramItemForm + indslag-tilknytning

**Action**: Opret `src/components/admin/ProgramItemForm.tsx` (Client Component):

Felter:
- **Titel** (text, required)
- **Type** (select: break/performance/info/ceremony)
  - Labels: break → Pause, performance → Indslag, info → Information, ceremony → Ceremoni
- **Starttid** (datetime-local, optional)
- **Varighed** (number — minutter, optional)
- **Indslag** (select, KUN synligt hvis type = 'performance'):
  - Dropdown med `performances` der har status `approved` eller `scheduled`
  - Viser: gæstenavn — titel (varighed)
  - Placeholder: "— Vælg indslag —"
- **Overordnet punkt** (select, optional):
  - Dropdown med alle top-niveau programpunkter (parent_id = null)
  - Bruges til at neste et punkt under et andet
  - Vis ikke som option hvis redigerer et punkt der selv har børn
- **Noter** (textarea, optional)

Props:
- `item?: ProgramItem` — til redigering (pre-udfylder formularen)
- `performances: Performance[]` — liste af tilgængelige indslag
- `topLevelItems: ProgramItem[]` — til parent_id select
- `defaultParentId?: string` — forvælgt forælder (fra "Tilføj underordnet punkt")
- `onSave: () => void`

Submit:
- Ny: kalder `createProgramItem(formData)`
- Rediger: kalder `updateProgramItem(id, formData)`
- Ryd og kald `onSave()` efter success

Integrer `ProgramItemForm` i `ProgramManager`:
- "Nyt programpunkt"-knap viser formular i Sheet
- "Rediger"-knap viser formular pre-udfyldt
- "Tilføj underordnet punkt"-knap viser formular med `defaultParentId` sat

**Verify**: `npx tsc --noEmit` — ingen fejl.

**Done**: Programpunkter kan oprettes med indslag-tilknytning og nesting. Fuld CRUD fungerer.

---

## Output
- `src/lib/actions/program.ts`
- `src/app/(admin)/admin/program/page.tsx`
- `src/components/admin/ProgramManager.tsx`
- `src/components/admin/ProgramItemForm.tsx`

## Success Criteria
- [ ] Programpunkter kan oprettes med alle felter
- [ ] Programpunkter kan redigeres og slettes
- [ ] Op/ned-flytning fungerer korrekt (sort_order swap)
- [ ] Nesting: underordnede punkter vises under forælder
- [ ] Type 'performance' viser indslag-select med godkendte/planlagte indslag
- [ ] Sletning af forælder sletter også børn
- [ ] TypeScript kompilerer rent (`npx tsc --noEmit`)
